package kube

// import (
// 	"k8s.io/client-go/tools/cache"
// )

// // Controller is a controller for watching over pods
// type Controller struct {
// 	store    cache.Indexer
// 	informer cache.Controller
// }

// // newController returns a new controller
// func newController(store cache.Indexer, informer cache.Controller) (*Controller, error) {

// 	return &Controller{store, informer}, nil
// }

// // run runs the controller
// func (ctrl *Controller) run(stopCh <-chan struct{}, numWorkers int) {

// }
